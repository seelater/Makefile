# Makefile
c proj makefile example with dependencies check. touch .h  

# proj struct

./			- root 

./source	- .c files

./include	- .h files

./lib     -     libs.  
EXAMPLE:  
cp libname.a ./lib/  
after LIBS  = -L$(LIB_DIR) add -lname  

./Makefile  - 
